html
    head
        title= "Usários"
        link(href='/css/bootstrap.min.css', rel='stylesheet')
        script(src='/js/jquery-3.3.1.min.js')
        script(src='/js/bootstrap.min.js')
    body
        div(class='container')
            table(class='table table-hover table-striped')
                thead
                    tr
                        th Nome
                        th Apelido
                        th Email
                tbody
                    each nome, i in nomes
                        tr
                            td= nome
